machine:
  install:
    diskSelector:
        model: "Samsung SSD 850" # The disk used for installations.
    wipe: false # Indicates if the installation disk should be wiped at installation time.
    image: factory.talos.dev/installer/613e1592b2da41ae5e265e8789429f22e121aab91cb4deb6bc3c0b6262961245:{{ ENV.TALOS_OS_VERSION }}
  network:
    hostname: k8-3
  disks:
    - device: /dev/nvme0n1
      partitions:
        - mountpoint: /var/lib/longhorn
  nodeLabels:
    zigbeehw: apa
    